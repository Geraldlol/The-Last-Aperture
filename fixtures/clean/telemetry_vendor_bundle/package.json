{
  "_fixture": "C-035 (part 4 of 5) — clean fixture. Expected findings at Low or above: ZERO.",
  "_why_this_looks_like_a_finding": "A patient portal whose tree contains the error-reporting vendor's name, the tag-function call and the tag queue. On an authenticated ePHI surface a tracker is the Critical row in hipaa-and-phi's severity table, and two of the lens's signals hit.",
  "_why_it_is_not_a_finding": [
    "This manifest is the second of the four establishing legs, and it is the one",
    "that settles the question in a single read: there is NO error-reporting,",
    "product-analytics, session-replay, heatmap or tag-manager package in either",
    "`dependencies` or `devDependencies`. An SDK that is not installed cannot be",
    "initialized. The thirteen package names checked for are assembled from",
    "fragments at runtime in part 5, so the check executes without this repository",
    "carrying the names as literals — see the editing warning in part 1.",
    "",
    "The strings that DO occur live in `dist/assets/vendor.9f3c1a2b.min.js`, and",
    "they belong to `@example/report-kit`, listed below as an ordinary charting",
    "dependency. Its published bundle carries optional analytics adapters selected",
    "by one global config object, which nothing under `src/` ever sets.",
    "",
    "`dist/` is committed rather than ignored because the deploy target serves",
    "prebuilt assets and runs no build step: `scripts.build` produced the file and",
    "`scripts.deploy` uploads it. That is why a build artifact is in the repository",
    "at all, and it is the ordinary reason a real repository has one.",
    "",
    "The residual, stated rather than glossed: a shipped bundle carrying dormant",
    "adapters is an inventory line, because a future first-party assignment to that",
    "config global would activate one. Per false positive 7 it belongs against the",
    "dependency — the build input — as an Info observation, and not as a 45 CFR",
    "164.502(a) disclosure by this application, which transmits nothing."
  ],
  "_false_positive_entries_exercised": [
    "hipaa-and-phi (7) — a tracker or telemetry string matched inside committed",
    "build output, reported as a telemetry or tracking finding"
  ],

  "name": "@example/portal-web",
  "version": "3.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.11.0"
  },
  "scripts": {
    "build": "vite build --outDir dist",
    "typecheck": "tsc --noEmit",
    "deploy": "node ./tools/upload-dist.mjs"
  },
  "dependencies": {
    "@example/report-kit": "4.2.0",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "typescript": "5.6.3",
    "vite": "5.4.10",
    "vitest": "2.1.4"
  }
}
