// Release-disabled compatibility stub. The manifest grants no permissions and
// does not register a background worker. Retained only so historical references
// fail visibly instead of silently activating an obsolete transport.
export const HTTP_AUTHED_BROWSER_BRIDGE_DISABLED = true
