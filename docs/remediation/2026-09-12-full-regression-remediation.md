# Full Regression Remediation Ledger

This file is the durable handoff for the repository-wide remediation that began on 2026-09-12. Read it before resuming after context compaction or handing work to another agent. Update it whenever a finding changes state, a validation result changes, or a commit lands.

## Objective

Repair every reproducible correctness, durability, security, packaging, CI, and runtime issue found in the full repository review. The work is complete only when the focused regressions and the full supported validation matrix pass, the shipped artifact has a unique version, the installed Codex and Claude skills resolve to the repaired tree, and remaining external runtime dependencies are either validated live or recorded with exact evidence.

## Repository state

- Repository: `C:\Users\geral\Red Team\last-aperture`
- Branch: `fix/full-regression-remediation`
- Original remediation base: `1fa73692e9b86cb30e11ebfd7d49f943da1eaf63`
- Rebased release base: `594e50f` (`origin/main`, license-owner update)
- Base PR: `#8`
- Remediation PR: `#9` (`Release Last Aperture 0.14.1 with full regression hardening`)
- Review source reconciled: `C:\Users\geral\.codex\attachments\4e191ac4-a3dc-47bc-9023-39ca5a1edad2\pasted-text.txt`
- Review source SHA-256: `3ff3f1a0255b0fab871f20ca3d996db9357f78e516257a26b8b089feb3c887de`
- Baseline full suite: 2,965 tests; 2,962 passed, 1 failed, 2 skipped.
- Baseline failure: authenticated campaign stop-marker publication race.
- GitHub main run: Node 24 failed the Ghidra Windows-path test; Node 20 failed that test and cancelled timer-dependent tests.

## Review-source reconciliation

Every release-blocker statement in the pasted review is represented below. This mapping is the completeness check for future context compactions; the detailed acceptance criteria remain in the workstream tables.

| Pasted-review statement | Ledger coverage |
| --- | --- |
| Evidence metadata can be changed without invalidating the root. | `EVID-01` |
| HAR import persists credentials from URLs and bodies. | `WEB-01` |
| Dead-process HTTP recon locks prevent recovery. | `HTTP-01` |
| Repeated recon finalization corrupts valid evidence. | `HTTP-04` |
| Emergency-stop publication races readers and late runtime disposal. | `HTTP-03` |
| Handled redirects become fatal after ledger reopen. | `HTTP-02` |
| Repeatedly encoded traversal can escape the approved path. | `WEB-02` |
| Connector deadlines omit credential callbacks. | `WEB-05` |
| Evidence verification reads files before enforcing bounds. | `EVID-02` |
| Provably unsent recon requests can be marked ambiguous. | `HTTP-05` |
| Recon report creation is non-atomic. | `HTTP-06` |
| Authenticated rate limits reset across reopen. | `HTTP-07` |
| Failed mutation reads retain response or PHI buffers. | `HTTP-08` |
| Unrefed recon and connector timers allow premature process exit. | `HTTP-09`, `WEB-05` |
| Oversized browser responses can hang in `reader.cancel()`. | `WEB-04` |
| HAR body limits trust caller-controlled `postData.size`. | `WEB-03` |
| Persisted acquisition plans can substitute commands. | `EVID-04` |
| Bundle output can follow hard links outside the bundle. | `EVID-03` |
| Command, object, registry-download, and decompression limits occur after consumption. | `EVID-05`, `EVID-08`, `OCI-01` |
| Acquisition finalization is not bound to state, adapter, or root. | `EVID-06` |
| Registry credentials are unused and pulled identity is not matched to the requested digest. | `EVID-07` |
| Crafted replay buffers whole responses and treats read failures as success. | `WEB-06` |
| Crafted scans permit state-changing GET routes without settlement. | `WEB-07` |
| Passive scans overclaim coverage without response evidence. | `WEB-08` |
| OCI normalization mishandles normal paths, gzip, repeated or large layers, PAX metadata, and directories. | `OCI-01` through `OCI-04` |
| Main CI failed on Node 20 and 24 and PR 8 merged without status checks. | `PLAT-01`, `CI-01`; hosted PR gate in completion checklist |
| Baseline local suite, focused suite, Python conformance, lint, generators, and audit outcomes. | Validation log and completion checklist |
| Ghidra succeeded while Frida and Burp were unavailable; Docker daemon was stopped. | `RUN-01` through `RUN-04`, with current remediation evidence in the validation log |
| The existing 0.14.0 archive and newer 0.14.0 source are different builds. | `REL-01` |
| The reviewed baseline was clean and no files were changed by that review. | Repository base above; remediation changes are tracked by this branch and validation log |

## Resume procedure

1. Read this ledger.
2. Run `git status --short --branch` and `git log --oneline -10`.
3. Inspect unchecked or `IN PROGRESS` findings and their named tests.
4. Run the smallest relevant test before editing, then add a failing regression and repair it.
5. Record the focused command and outcome here after each slice.
6. Do not claim completion until the full validation and release checklist at the end is checked.

Status values: `OPEN`, `IN PROGRESS`, `FIXED`, `VERIFIED`, `DEFERRED`.

## Earlier Claude review regression retention

These four findings were repaired before the current remediation base. They remain explicit release requirements so later refactors cannot silently reintroduce them.

| ID | Status | Finding | Required proof |
| --- | --- | --- | --- |
| BASE-R1 | VERIFIED | HTTP 304, 305, and 306 responses must not be treated as followable redirects that terminate the remaining authenticated campaign. | Independent controller, ledger-reopen, and mutation status matrices on Node 24 and Node 20 retain all three exclusions without terminating the remaining campaign. |
| BASE-R2 | VERIFIED | Passive-only bounty review must accept captured state-changing methods because it sends no request. | The state-changing guard applies only when a crafted class is active; captured-POST regression proves zero target sends and passes. |
| BASE-R3 | VERIFIED | Body truncation followed by a rejecting or nonsettling `reader.cancel()` must remain a valid bounded observation and cannot abort the browser session. | Independent rereview confirms injected-fetch cancellation returns `RESPONSE_BOUNDED` with `response_truncated=true`; Node 24 and Node 20 browser matrices pass. |
| BASE-R4 | VERIFIED | OCI normalization must accept bounded GNU base-256 numeric fields and convert appended/repeated outer members into a documented gap rather than an uncaught exception. | Independent Evidence/OCI rereview confirms bounded base-256 size/mtime parsing and named duplicate-member gaps on Node 24 and Node 20. |

## HTTP campaign and recon lifecycle

| ID | Status | Finding | Primary code | Required proof |
| --- | --- | --- | --- | --- |
| HTTP-01 | VERIFIED | Recover a recon run when its lock belongs to a dead process or is demonstrably stale. | `scripts/lib/http-recon-controller.mjs` | Independent dead/live, identity-safe contention, and repeated stale-reclaim matrices pass on Node 24 and Node 20. |
| HTTP-02 | VERIFIED | Preserve handled redirect semantics when an authenticated campaign ledger is reopened. | `scripts/lib/http-authed-campaign-ledger.mjs` | Independent reopen and failed handled-stop append regressions pass on Node 24 and Node 20. |
| HTTP-03 | VERIFIED | Publish campaign stop markers atomically and dispose runtimes if stop monitoring fails. | `scripts/lib/http-authed-campaign-ledger.mjs`, `scripts/lib/http-authed-campaign-runtime.mjs` | Independent atomic-publication, late-cleanup, and repeated-concurrency regressions pass on Node 24 and Node 20. |
| HTTP-04 | VERIFIED | Make rooted recon finalization idempotent; a late stop must not corrupt an already valid bundle. | `scripts/lib/http-recon-controller.mjs` | Independent re-finalize and late-stop regressions pass on Node 24 and Node 20. |
| HTTP-05 | VERIFIED | Mark recovery ambiguous only after durable pre-dispatch evidence, not after a merely queued/SENT state. | `scripts/lib/http-recon-controller.mjs` | Independent crash-before-dispatch regressions remain provably unsent on Node 24 and Node 20. |
| HTTP-06 | VERIFIED | Publish recon reports atomically. | `scripts/lib/http-recon-controller.mjs` | Independent platform-specific durable-move and failure regressions pass on Node 24 and Node 20. |
| HTTP-07 | VERIFIED | Preserve authenticated campaign and mutation rate-limit timing across reopen. | authenticated campaign and mutation controllers/ledgers | Independent reopen-timing regressions pass on Node 24 and Node 20. |
| HTTP-08 | VERIFIED | Zero mutation response buffers on every early status and recovery path. | `scripts/lib/http-authed-mutation-controller.mjs` | Independent instrumented zeroization regressions pass on Node 24 and Node 20. |
| HTTP-09 | VERIFIED | Keep stop-watcher timers alive while callers are awaiting them. | `scripts/lib/http-recon-controller.mjs` | Independent lifecycle matrices complete without failure or cancellation on Node 24 and Node 20. |

## Browser, HAR, bounty, and connector runtime

| ID | Status | Finding | Primary code | Required proof |
| --- | --- | --- | --- | --- |
| WEB-01 | VERIFIED | Remove credentials from query strings and structured/request bodies before persisting imported authz requests. | bounty authz request/controller | Independent credential-carrier and legacy persistence review passes on Node 24 and Node 20. |
| WEB-02 | VERIFIED | Reject repeatedly encoded traversal and separator tokens consistently across engagement, route, HAR, and recon contracts. | engagement contracts/registry, `reverse-web-har.mjs`, recon contracts | Independent layered and mixed-encoding review passes on Node 24 and Node 20. |
| WEB-03 | VERIFIED | Bound HAR request bodies by actual decoded text/byte length; do not trust `postData.size`. | `scripts/lib/reverse-web-har.mjs` | Independent text, params, and forged-size review passes on Node 24 and Node 20. |
| WEB-04 | VERIFIED | Bound injected-fetch cancellation; a hostile `reader.cancel()` cannot hang the observation. | `browser/http-authed-chrome/injected-fetch.mjs` | Independent absolute read/cancel deadline review passes on Node 24 and Node 20. |
| WEB-05 | VERIFIED | Include credential-provider and receiver execution in generated connector deadlines, and keep deadline timers live. | `scripts/templates/native-connector-runtime.mjs` | Independent provider/receiver/deadline/retry ambiguity review passes on Node 24 and Node 20. |
| WEB-06 | VERIFIED | Stream and bound crafted bounty responses; do not turn response-read failure into successful evidence. | bounty replay/scan controller | Independent oversize, UTF-8 expansion, read-failure, cancellation, and settlement review passes on Node 24 and Node 20. |
| WEB-07 | VERIFIED | Detect state-changing routes as well as HTTP verbs before crafted dispatch. | bounty scan plan/controller | Independent mutation-route and method-override review passes on Node 24 and Node 20. |
| WEB-08 | VERIFIED | Passive scans must not claim full coverage without response evidence. | bounty scan controller/import | Independent review confirms request-only input remains incomplete and passive mode performs no target I/O. |

## Evidence acquisition and OCI

| ID | Status | Finding | Primary code | Required proof |
| --- | --- | --- | --- | --- |
| EVID-01 | VERIFIED | Bind provenance, coverage/status, confidence, evidence IDs, attestation, and ordered records into the evidence root. | `scripts/lib/evidence-bundle.mjs` | Independent rereview confirms the v2 root covers the exact canonical manifest projection and ordered records; metadata/root-binding and legacy-v1 regressions pass on Node 24 and Node 20. |
| EVID-02 | VERIFIED | Enforce manifest and per-payload byte limits before unbounded reads. | evidence bundle/schema | Independent rereview confirms opened-handle reads stop at `limit + 1`; manifest and payload oversize regressions pass on Node 24 and Node 20. |
| EVID-03 | VERIFIED | Refuse symlink/hardlink/reparse-point payload targets and publish payloads safely. | `scripts/lib/evidence-bundle.mjs` | Independent rereview confirms single-link ancestor binding, durable create-only publication, and hard-link/junction/partial-output refusal. |
| EVID-04 | VERIFIED | Cryptographically bind executable acquisition commands and immutable adapter identity to approved plans. | evidence acquire controller/adapters | Independent rereview confirms plan/adapter/version/execution-profile/command identity binding and pre-dispatch drift refusal. |
| EVID-05 | VERIFIED | Enforce command, object, stdout, stderr, and total-byte caps before side effects exceed policy. | evidence CLI runner and live/deployed adapters | Independent rereview confirms safe-integer preflight, streaming `cap + 1` termination, truthful attempted counters, and exhausted-budget refusal. |
| EVID-06 | VERIFIED | Finalize only executed, matching plans and bind adapter/class/root to the final bundle. | evidence acquire controller | Independent rereview confirms immutable receipt/state/finalize binding to the caller-pinned adapter, plan digest, run-returned receipt digest, seed, context, and sealed root. |
| EVID-07 | VERIFIED | Resolve registry credential references, avoid leaking secrets, and verify the requested digest. | image reference/registry adapter | Independent rereview confirms built-in environment lookup, pre-probe credential isolation, failure redaction, and requested manifest/blob digest and size checks. |
| EVID-08 | VERIFIED | Bound registry output while it is written. | registry adapter | Independent rereview confirms each Crane read is streamed under per-object and aggregate stdout/stderr/total budgets before JSON parsing. |
| EVID-09 | VERIFIED | Reject stateful SOQL clauses such as `FOR UPDATE`, `FOR VIEW`, and `FOR REFERENCE`. | evidence readonly allowlist | Independent stateful-clause and adapter matrices pass on Node 24 and Node 20. |
| OCI-01 | VERIFIED | Bound gzip inflation and normalize the decoded outer archive exactly once. | `scripts/lib/oci-normalizer.mjs` | Independent rereview confirms aggregate inflation accounting, decoded-content caching, and reuse of the verified outer archive index. |
| OCI-02 | VERIFIED | Accept normal tar names such as `./file` and size ordinary OCI layer members by policy rather than the generic 8 MiB entry cap. | OCI normalizer | Independent `./` path and realistic large-layer policy regressions pass on Node 24 and Node 20. |
| OCI-03 | VERIFIED | Preserve repeated ordered layers instead of deduplicating by digest/name. | OCI normalizer | Independent rereview confirms every descriptor occurrence remains ordered while verified and decoded bytes are cached. |
| OCI-04 | VERIFIED | Treat directory entries correctly and support or reject PAX/GNU metadata explicitly without false orphan findings. | OCI normalizer | Independent rereview confirms directory suppression, GNU base-256, duplicate-member gaps, and explicit bounded x/g/L/K handling. |

## Independent second-pass additions

These defects were reproduced against the remediation working tree. They are release blockers even where they refine an original finding above.

| ID | Status | Finding | Required proof |
| --- | --- | --- | --- |
| HTTP-R1 | VERIFIED | Windows directory `fsync` returns `EPERM`; recon reports and campaign stop/cleanup publication must use an honest durable Win32 primitive rather than treating that error as success. | Independent rereview confirms controller-owned `MoveFileExW` write-through replacement/create-only publication and helper lifecycle on real Windows under Node 24 and Node 20. |
| HTTP-R2 | VERIFIED | Failure to append a handled-response stop after durable `REQUEST_SETTLED` can emit `FAILED_BEFORE_SEND`, continue live execution, and disagree with the reopened projection. | Independent rereview confirms settled redirects and durable discovered candidates reconcile consistently in live and reopened projections. |
| HTTP-R3 | VERIFIED | Recon lock collision/release leaks raw `ENOENT`, while stale reclamation can move a replacement live owner. | Independent rereview confirms exclusive reclaim serialization and identity checks; all ten 24-way stress repetitions pass. |
| HTTP-R4 | VERIFIED | Recon artifact and stale-lock inspection trusts a pathname `lstat` across the later open and can read beyond its declared bound after a swap or concurrent growth. | Independent rereview confirms opened-handle and endpoint identity are bound around a fixed `maxBytes + 1` read; deterministic swap coverage passes. |
| HTTP-R5 | VERIFIED | Authenticated ledger file inspection has the same path-swap and concurrent-growth window and calls unbounded `readFile()` before detecting the mismatch. | Independent rereview confirms opened-handle and endpoint identity are bound around a fixed `maxBytes + 1` read; deterministic record-swap coverage passes. |
| HTTP-R6 | VERIFIED | A late error after durable JSON-observation settlement can terminalize the action with an invalid success outcome, while an error after its durable stop double-counts the action as both failed and completed. | Independent rereview confirms terminal outcome and telemetry derive from the durable failure-stage record and increment exactly one result bucket. |
| HTTP-R7 | VERIFIED | Campaign scope and synthetic-body reads accept hard links and can consume an arbitrarily grown or swapped file through `readFile()` after checking an earlier pathname. | Independent rereview confirms single-link input enforcement, opened-handle binding, and fixed original-size-plus-one reads before credential or target I/O. |
| HTTP-R8 | VERIFIED | HTTP recon accepts hard-linked evidence artifacts even though an alias can mutate the same inode outside the bundle. | Independent rereview confirms evidence inputs require one link while only controller lock-publication transitions admit two names. |
| HTTP-R9 | VERIFIED | Authenticated-ledger open can misclassify the normal gap between lock-directory creation and owner publication as corruption, and an acquisition failure leaves that partial directory blocking future opens. | Independent rereview and repeated Node-20 runs confirm bounded incomplete-owner retries and identity-bound partial-lock cleanup. |
| HTTP-R10 | VERIFIED | HTTP recon writes directly into its final stop-marker name, so a concurrent requester or watcher can consume an empty/partial marker and substitute a fail-closed fallback record. | Independent rereview confirms fully written, fsynced, create-only publication; concurrent requesters observe one complete canonical marker. |
| HTTP-R11 | VERIFIED | A crashed HTTP-recon reclaim-guard owner, including a crash during direct owner-record publication, can leave `.http-recon.lock-reclaim` permanently blocking every future stale-run-lock recovery. | Independent rereview confirms private temporary publication, valid two-name crash-tail recovery, and identity-matched dead-owner reclamation. |
| HTTP-R12 | VERIFIED | Recon and authenticated-ledger lock release verify the well-known pathname and then delete through that same pathname, allowing a replacement live owner to be removed between those operations. | Independent rereview confirms private-name quarantine, captured identity validation, mismatch restoration, and removal of only the quarantined inode. |
| HTTP-R13 | VERIFIED | A bounded stop-marker read can lose its pathname after the initial `lstat`, bubble raw `ENOENT`, and let the authenticated stop reader interpret a raced marker as absent. | Independent rereview confirms post-`lstat` disappearance becomes a typed change and authenticated inspection fails closed after bounded transition retries. |
| HTTP-R14 | VERIFIED | Authenticated stop inspection accepts any same-inode hard-link alias whose name merely starts with the controller temporary prefix. | Independent rereview confirms only the exact PID-and-24-hex controller grammar explains a two-link transition; malformed aliases fail closed. |
| HTTP-R15 | VERIFIED | A browser or ledger finalizer error replaces an earlier campaign startup/execution failure, hiding the primary cause and potentially skipping later cleanup. | Independent rereview confirms every resource disposal runs and typed aggregates preserve the operation error first. |
| HTTP-R16 | VERIFIED | On Windows, a concurrent lock waiter can briefly hold `owner.json` open and make an identity-bound lock-directory quarantine fail with a sharing violation. | Independent rereview confirms only bounded Win32 sharing codes retry before captured-identity validation; 20 repeated Node-20 publication/release runs pass. |
| HTTP-R17 | VERIFIED | Discovery response chunks remained live when probe settlement or processing failed before the discovery branch, including a transport failure after response observation but before runtime handoff. | Independent integration review confirms returned and pre-handoff response chunks are erased on exceptional paths; the six-suite HTTP matrix passes 229/229 on Node 24 and Node 20. A separate observer-handoff gap is tracked as HTTP-R19. |
| HTTP-R18 | VERIFIED | Probe, native HTTPS, mutation, and recovery paths retained controller-created request-body or credential copies after transport settlement, while shape-only observation retained response chunks without a downstream owner. | Independent integration review confirms outbound copies and locally consumed observation chunks are erased after settlement on Node 24 and Node 20. Rejected preflight copies are separately tracked as HTTP-R20. |
| HTTP-R19 | VERIFIED | Failed JSON-shape observation or a throwing downstream observer retained response chunks because cleanup ownership was inferred from the observer's presence rather than a successful handoff. | Independent rereview confirms cleanup ownership transfers only after a downstream observer resolves; shape failure, no downstream owner, and observer rejection erase every retained chunk on Node 24 and Node 20. |
| HTTP-R20 | VERIFIED | Probe and mutation body-binding rejection, oversized mutation credentials, and recovery body preflight left allocated byte copies outside cleanup ownership. | Independent rereview retains every rejected preflight copy, proves zero dispatches, and confirms erasure on Node 24 and Node 20. |
| HTTP-R21 | VERIFIED | Mutation recovery did not compare supplied credential bytes with the sealed credential digest before cleanup dispatch. | Independent rereview confirms the sealed credential digest is checked inside recovery cleanup ownership and before any transport dispatch on Node 24 and Node 20. |
| HTTP-R22 | VERIFIED | Native authenticated HTTPS transport timer setup occurred before body-copy cleanup ownership, while a throwing timer cleanup skipped body erasure. | Independent rereview confirms nested cleanup erases the transport's private request-body copy when timer setup or cleanup throws on Node 24 and Node 20. |
| HTTP-R23 | VERIFIED | Native authenticated HTTPS transport cloned response chunks without an observer and retained observation-prefix and overflow clones when the response byte limit failed. | Independent rereview confirms no observer means no response copy, and limit failure erases every retained observation prefix before settlement on Node 24 and Node 20. |
| HTTP-R24 | VERIFIED | Native authenticated HTTPS response observation created a second set of handoff copies that a rejecting or nonsettling observer could remove from the passed array and keep live after failure or timeout. | Independent post-fix review confirms the private tracker remains authoritative after passed-array mutation and erases retained copies on rejection and deadline failure on Node 24 and Node 20. |
| HTTP-R25 | VERIFIED | The mutation campaign response observer copied handed-off transport chunks but did not erase the input chunks after assuming ownership. | Independent post-fix review confirms every transport-owned mutation observation input is erased in `finally` after the bounded internal copy on Node 24 and Node 20. |
| HTTP-R26 | VERIFIED | The browser bridge passed a response-body copy only through the observer argument, so session close or timeout could not erase a copy removed from that array while the observer remained pending. | Independent post-fix review confirms close and timeout erase privately tracked observer copies after passed-array mutation on Node 24 and Node 20. Concurrent RESULT replacement is separately tracked as HTTP-R29. |
| HTTP-R27 | VERIFIED | Browser transport timer setup occurred after `session.prepare()`, so a throwing timer dependency left the prepared request-body copy and in-flight action live. | Independent post-fix review confirms timer-setup failure erases prepared bytes, removes the action, restores the open session, and reports a typed pre-send failure on Node 24 and Node 20. |
| HTTP-R28 | VERIFIED | A throwing browser timer cleanup interrupted waiter settlement and session close before the paired session secret was erased. | Independent post-fix review confirms timer cleanup is best-effort after action detachment, so waiters settle and request/session-secret copies are erased on Node 24 and Node 20. Reused-ID stale callback isolation is separately tracked as HTTP-R30. |
| HTTP-R29 | VERIFIED | Two concurrent browser `RESULT` messages could both enter response observation while the action remained in `COMMIT`; the second replaced the first private cleanup tracker, so close or timeout could leave the first observer's shifted response copy live. | Independent post-fix review confirms the first response claims observation before its asynchronous observer boundary, the duplicate is rejected, and close erases the sole privately tracked shifted copy on Node 24 and Node 20. |
| HTTP-R30 | VERIFIED | Best-effort browser timer cleanup treated a stale callback as inert by comparing only its action ID, so an uncleared callback could cancel a later action when the public action-ID factory reused the same ID. | Independent post-fix review confirms timer ownership binds to the exact active-action object, so an uncleared callback cannot affect a later action even when its external ID is reused on Node 24 and Node 20. |
| HTTP-R31 | VERIFIED | A throwing native HTTPS timer cleanup replaced an already settled response or primary transport error, allowing a sent request to be misclassified as failed before send. | Timer cleanup is best-effort inside body-copy cleanup ownership; deterministic success, timeout, and primary-error regressions pass on Node 24 and Node 20. Independent Node 24.13.0 and portable Node 20.20.2 post-fix review verifies this row. |
| HTTP-R32 | VERIFIED | Chrome companion controller timer setup could reject outside the request boundary, while throwing cleanup replaced successful and timed-out controller exchanges. | Setup failure is typed before fetch and cleanup cannot replace the authoritative exchange outcome; deterministic setup, success, and timeout regressions pass on Node 24 and Node 20. Independent Node 24.13.0 and portable Node 20.20.2 post-fix review verifies this row. |
| HTTP-R33 | VERIFIED | Loopback bridge attach timer setup could strand a shared waiter, while throwing cleanup interrupted successful attach or close settlement. | Each timer callback binds its exact waiter, setup failure resets retryable state, and cleanup is best-effort after waiter detachment; setup, attach, and close regressions pass on Node 24 and Node 20. Independent Node 24.13.0 and portable Node 20.20.2 post-fix review verifies this row. |
| HTTP-R34 | VERIFIED | Recon timer setup failure retained an external stop listener, while throwing cleanup replaced successful or timed-out network outcomes. | Timer ownership now begins inside the listener-cleanup scope and cleanup cannot replace the primary outcome; setup, success, and sent-timeout regressions pass on Node 24 and Node 20. Independent Node 24.13.0 and portable Node 20.20.2 post-fix review verifies this row. |
| HTTP-R35 | VERIFIED | The authenticated HTTPS transport leaked shared recon timeout codes during DNS resolution, so dispatch replaced a real unsent timeout with a generic transport failure. | The authenticated transport now translates shared recon abort and timeout failures at its DNS boundary into typed `HTTP_AUTHED_TIMEOUT` errors with `request_may_have_been_sent:false`; deterministic synchronous pre-DNS and DNS-pending regressions pass on Node 24 and Node 20. Independent Node 24.13.0 and portable Node 20.20.2 post-fix review verifies this row. |
| HTTP-R36 | VERIFIED | A native response observer could transfer its handed-off body buffer and freeze the callback-visible array before rejecting, causing cleanup to throw on detached storage or frozen external state instead of settling the typed observer failure. | Private cleanup trackers remain authoritative, erase each still-owned reachable byte copy through a no-throw intrinsic, never mutate the external array, and always settle the request; the 16 KiB transfer-and-freeze rejection regression passes on Node 24 and Node 20. Independent Node 24.13.0 and portable Node 20.20.2 post-fix review verifies this row. |
| HTTP-R37 | VERIFIED | A browser bridge response observer could transfer its handed-off `RESULT` body buffer and freeze the callback-visible array before rejecting, causing cleanup to strand the action or waiter. | Bridge cleanup drops external references without mutation, erases each still-owned reachable private copy best-effort, clears action state, and settles both `acceptResult` and the transport waiter with the typed observer failure; the 16 KiB transfer-and-freeze regression passes on Node 24 and Node 20. Independent Node 24.13.0 and portable Node 20.20.2 post-fix review verifies this row. |
| HTTP-R38 | VERIFIED | Native request-body copies were shared with `request.end` or an injected transport, and the bounded observer wrapper invoked callback-visible `.fill`, so detachment or method override could replace a successful response or the primary failure during cleanup. | Request and observer cleanup now uses a captured no-throw byte intrinsic; deterministic native-send detachment, injected-transport detachment, and hostile observer-fill regressions preserve the authoritative outcome and erase still-reachable owned bytes on Node 24 and Node 20. Independent Node 24.13.0 and portable Node 20.20.2 post-fix review verifies this row. |
| HTTP-R39 | VERIFIED | The loopback bridge handed its JSON response buffer to `response.end`, then an unguarded finish/close scrub could throw if the HTTP dependency detached that storage. | Finish and close now share an idempotent no-throw intrinsic scrub, so event callbacks cannot overturn the completed response; the deterministic detached-handoff regression passes on Node 24 and Node 20. Independent Node 24.13.0 and portable Node 20.20.2 post-fix review verifies this row. |
| HTTP-R40 | VERIFIED | Native recon and its exported DNS resolver trusted caller-provided abort-listener setup and cleanup: an add that retained then threw leaked the binding, while a throwing removal could replace success, replace a primary setup failure, or strand DNS settlement. | Both listener boundaries now track every attempted binding inside cleanup-owned scopes, remove it best-effort after setup failure or settlement, and never let removal override the authoritative result. Deterministic add-retain-then-throw, timer-failure, DNS-success, and request-success regressions pass on Node 24 and Node 20. Independent Node 24.13.0 and portable Node 20.20.2 post-fix review verifies this row. |
| HTTP-R41 | VERIFIED | Node 24.20 on hosted Linux marks Node-managed `Buffer` storage as non-transferable, so the request-body detachment regressions threw `DataCloneError` instead of exercising successful transport handoff and no-throw cleanup. | Request-body boundaries now preflight their byte limits, copy into dedicated full-span `ArrayBuffer` storage, preserve caller ownership, erase temporary and partially copied storage on failure, and tolerate downstream detachment. Local focused review passes on Node 24.20.0 and Node 20.20.2; both detachment regressions run and pass on hosted Linux under Node 24.20.0 and Node 20.20.2 in Actions run `34718095126`. |
| EVID-R1 | VERIFIED | Acquisition plan mutation is in-place, link-following, unbounded, and unserialized; concurrent runs both dispatch and stop parse faults fail open. | Independent rereview confirms immutable plan/state separation, exclusive run locking, bounded fail-closed reads, and hard-link/run-run/run-stop regressions. |
| EVID-R2 | VERIFIED | Registry secrets remain persistable when the provider returns the existing environment object and omits optional redaction values. | Independent rereview confirms environment-locator values become mandatory secret material before provider mutation; failure evidence persists no target credential. |
| EVID-R3 | VERIFIED | A fabricated `ACQUIRED` record that never ran can finalize a copied bundle because finalization trusts the record's own digest and omits the complete seed. | Independent rereview confirms caller-pinned receipt and plan digests plus full seed/context/root binding; forged, copied, and mismatched executions fail closed. |
| EVID-R4 | VERIFIED | A junction at `payload/` verifies, and path-based ancestor checks remain swappable before open. | Independent rereview confirms the payload root and every ancestor are identity-bound before and after opened-handle reads; root/nested junction and swap regressions pass. |
| EVID-R5 | VERIFIED | Repeated OCI layers have only per-inflate bounds and can accumulate/reinflate far beyond a campaign-wide capture budget. | Independent rereview confirms aggregate inflation/capture budgets and decoded-layer caching preserve occurrence order without repeated expansion. |
| EVID-R6 | VERIFIED | Approved plans omit adapter version, execution-profile identity, exact initial command, and bounded rules for descriptor-derived commands. | Independent rereview confirms adapter/version/profile, executable identity, exact command, derived-read rules, and traversal limits are sealed and revalidated. |
| EVID-R7 | VERIFIED | `NaN`/`Infinity` limits disable caps, and live object caps apply only after a complete response is consumed. | Independent rereview confirms positive safe-integer validation and producer termination at object/record/byte `cap + 1`. |
| EVID-R8 | VERIFIED | The root algorithm and acquisition-plan shape changed under version 1 without legacy verification or migration. | Independent rereview confirms the committed v1 bundle verifies under v1 rules while loading unbound v1 metadata and executing v1 plans require explicit migration. |
| EVID-R9 | VERIFIED | Bundle publication can leave partial output and admits Windows aliases, ADS, reserved names, trailing dots, and case-fold collisions. | Independent rereview confirms portable preflight plus staged synced create-only publication leave no partial or aliased bundle. |
| EVID-R10 | VERIFIED | Normal controller/CLI use has no built-in resolver for `env:NAME` registry credentials. | Independent rereview confirms default environment resolution works without an injected provider and never exposes the value to identity probes. |
| EVID-R11 | VERIFIED | Registry failure bundles discard command, object, stderr, and attempted/capped byte counters. | Independent rereview confirms authentication, descriptor, identity, command-cap, and output-cap outcomes record only operations that reached dispatch. |
| EVID-R12 | VERIFIED | Malformed manifests can throw during root derivation after schema validation already failed. | Independent rereview confirms root derivation is gated on schema validity and malformed profile/item inputs return `valid:false`. |
| EVID-R13 | VERIFIED | Registry archive ordering uses locale collation and fails the canonical-ordering gate. | Independent canonical-ordering suite passes 9/9 on Node 24 and Node 20. |
| EVID-R14 | VERIFIED | A claimed `RUNNING` to `ACQUIRED` repair trusted an on-disk receipt digest that a caller could never receive from the real post-receipt, pre-state crash window. | Independent fault-window review confirms durable receipt publication leaves `RUNNING` without a caller-trusted digest and finalize fails closed on Node 24 and Node 20. |
| EVID-R15 | VERIFIED | Output-limit failures dropped the overflowing POSIX chunk from durable byte counters, and Windows could mislabel a combined-boundary overflow as a stdout overflow. | Independent counter review confirms bounded `cap + 1` accounting and combined-before-stream classification on Node 24 and Node 20. |
| EVID-R16 | VERIFIED | Registry object/command counters could increment before command-cap preflight or executable-identity validation, and could remain zero after a target object was actually attempted. | Independent zero-cap, identity-drift, authentication, and provider-failure review confirms command/object attempts are recorded together only at dispatch on Node 24 and Node 20. |
| EVID-R17 | VERIFIED | A verified evidence projection shallow-froze shared profile/context objects, so callers could mutate nested arrays after verification; legacy metadata could also be loaded without root binding. | Independent review confirms the verified manifest snapshot is deeply immutable and legacy v1 remains verification-only for migration on Node 24 and Node 20. |
| EVID-R18 | VERIFIED | POSIX process-group supervision was described as arbitrary process-tree containment and could wait through the full command timeout when a self-detached descendant held inherited stdio. | The runner advertises `POSIX_PROCESS_GROUP`, arms its bounded post-exit cleanup timer, and returns `EVIDENCE_CLI_TERMINATION_UNCONFIRMED` with `termination_confirmed:false`; probe classification preserves that outcome. Public acquisition remains `UNAVAILABLE`, and hostile-descendant activation requires an enrolled cgroup or PID-namespace adapter. The self-detached live cases ran and passed on hosted Linux under Node 20.20.0 and Node 24.20.0 in Actions run `34715712032`. |
| EVID-R19 | VERIFIED | A Windows Job helper result with unconfirmed termination left private request/stdout/stderr scratch on disk even after the helper closed its handles. | Independent Windows review confirms identity-bound scratch cleanup runs for unconfirmed results without upgrading termination; no provider-output file remains on Node 24 or Node 20. |
| EVID-R20 | VERIFIED | Artifact acquisition checked source paths before an unbounded/path-based read, allowing hard-link, reparse, ancestor, growth, and swap ambiguity. | Independent source-reader review confirms single-link opened-handle bounds and ancestor/path identity rechecks; hard-link, junction, growth, and replacement cases fail closed on Node 24 and Node 20. |
| EVID-R21 | VERIFIED | Registry executable identity bound only invocation path strings, and version probes inherited target credentials; replacing the same path could execute attacker bytes with the credential. | Independent review confirms bounded executable/script fingerprints, sanitized probes, and pre-dispatch identity checks reject same-path replacement before credential resolution on Node 24 and Node 20. |
| EVID-R22 | VERIFIED | Registry manifest/config output could reach the global 256 MiB CLI cap before the normalizer's JSON bound, while binding combined output to exact stdout length rejected valid Crane warning stderr. | Independent per-object cap review confirms oversize descriptors stop before dispatch while exact stdout plus bounded warning stderr remains valid on Node 24 and Node 20. |
| EVID-R23 | VERIFIED | Legal Linux OCI paths such as `CON`, colon/trailing-dot names, Unicode/case-fold collisions, and ancestor collisions could make optional content publication abort an otherwise valid acquisition on Windows. | Independent portable-publication review confirms unsafe or colliding content is skipped with sealed metadata and a named `PARTIAL` gap on Node 24 and Node 20. |
| EVID-R24 | VERIFIED | Thousands of individually valid captured layer files could overflow the evidence manifest/file-count publication budget after successful normalization. | Independent review confirms the plan-bound file cap retains a deterministic prefix, records `captured_files`, and gaps the remainder before publication on Node 24 and Node 20. |
| EVID-R25 | VERIFIED | Acquisition used a bare directory lock with no durable owner or dead-owner recovery, and released through the well-known pathname after an early identity check, so a crash could block the bundle forever and a replacement could be removed. | Independent review confirms create-only owner publication and restoration, live one-link ownership, dead two-link crash-tail recovery, serialized reclamation, external quarantine, hook-finally cleanup, and replacement preservation. Node 24 and Node 20 each pass the 175-test Evidence matrix, plus 45/45 and 27/27 focused adversarial lock assertions. |
| EVID-R26 | VERIFIED | A collection producer stopped at object `cap + 1` before capture, but the durable impact record retained `objects_touched: 0` and omitted the dispatched operation because only byte-cap failures and successfully returned commands were accounted. | Independent review confirms the durable cumulative `cap + 1` sentinel and the operation-local cap-halted audit row after a prior successful dispatch. Node 24 and Node 20 each pass the 175-test Evidence matrix, and two direct counter-boundary assertions pass. |
| OCI-05 | VERIFIED | Aggregate entry/inflation work was debited only after successful parsing or gunzip, and artifact capture reparsed the outer archive without consuming the budget. | Independent budget review confirms accepted headers are debited, failed gzip exhausts remaining inflation, and payload capture reuses the verified outer index on Node 24 and Node 20. |
| OCI-06 | VERIFIED | Type-5 or malformed directory entries carrying bytes could satisfy authoritative OCI metadata/blob/layer lookups even though extraction treats them as directories. | Independent type-semantics review confirms malformed directories and non-regular authoritative members fail closed on Node 24 and Node 20. |
| OCI-07 | VERIFIED | Breadth-first nested-index traversal reordered descriptor occurrences, placing a later top-level image before an earlier nested image. | Independent traversal review confirms depth-first hierarchical descriptor and repeated-manifest occurrence order on Node 24 and Node 20. |
| OCI-08 | VERIFIED | Repeated OCI descriptors and Docker layer paths rehashed the same large authoritative member for every occurrence despite decode caching. | Independent cache review confirms each authoritative OCI or Docker member is hashed once while every ordered occurrence remains validated and projected on Node 24 and Node 20. |
| OCI-09 | VERIFIED | Malformed or enormous top/nested manifest and layer arrays were iterated before aggregate caps, could throw on null descriptors, and let invalid layers evade the occurrence budget. | Independent cardinality review confirms valid and invalid descriptors share pre-iteration caps with aggregated gaps and null-safe projection on Node 24 and Node 20. |
| OCI-10 | VERIFIED | Untrusted `history` and `rootfs.diff_ids` arrays were spread without item/byte limits, enabling call-stack failure and oversized final metadata files. | Independent metadata review confirms iterative aggregate item/byte caps, durable usage, and explicit serialization gaps on Node 24 and Node 20. |
| OCI-11 | VERIFIED | Layer `entries.json` projection was count-only and pretty-serialized, so control-heavy names/link targets could exceed the bundle payload-file cap after normalization succeeded. | Independent projection review confirms compact ordered prefixes stay within plan-bound record/byte caps, report `projected_bytes`, and gap omitted content on Node 24 and Node 20. |
| OCI-12 | VERIFIED | Tar parsing accepted malformed trailers/checksums/directory shapes, and archive creation could allocate the complete output before enforcing the archive budget. | Independent structural review confirms malformed trailers, checksums, and directories fail closed and archive size is preflighted before allocation on Node 24 and Node 20. |
| WEB-R1 | VERIFIED | HAR redaction missed URL userinfo/fragments, malformed encodings, signed-request/SSO carriers, secondary headers, and unkeyed credential values. | Independent carrier matrices on Node 24 and Node 20 persist no credential material. |
| WEB-R2 | VERIFIED | Structured-body redaction reserialized credential-free JSON/form bodies and broke exact signatures. | Independent Node 24 and Node 20 regressions preserve credential-free JSON and form bodies byte-for-byte. |
| WEB-R3 | VERIFIED | Crafted replay could omit or encode sentinels and secondary sensitive headers, while UTF-8 replacement could exceed the byte cap. | Independent replay matrices refuse unresolved or escaped sentinels and bound encoded output on Node 24 and Node 20. |
| WEB-R4 | VERIFIED | Chrome service-worker controller reads/cancellation could outlive the 15-second deadline and retain buffered or late chunks. | Independent deadline and late-chunk regressions prove bounded cancellation and tracker cleanup on Node 24 and Node 20. |
| WEB-R5 | VERIFIED | Scan dispatch was recorded returned before bounded response-body observation completed. | Independent scan matrices prove durable settlement follows completed bounded observation on Node 24 and Node 20. |
| WEB-R6 | VERIFIED | Connector credential-provider failure after a retryable send lost delivery ambiguity and could expose forged error details. | Independent Node 24 and Node 20 regressions preserve delivery ambiguity and sanitize forged failure details. |
| WEB-R7 | VERIFIED | Legacy or direct authorization request bundles can bypass redaction metadata and replay persisted query, body, or secondary-header credentials. | Independent rereview confirms the replay sink receives no original secret; missing, encoded, escaped, and secondary-header sentinels fail closed on Node 24 and Node 20. |
| WEB-R8 | VERIFIED | Reverse HAR classifies method-override requests as reads, so a generated connector can automatically retry a state-changing request. | Independent rereview covers method and routing headers, compound actions, 16 encoding layers, excessive layering, contract classification, and runtime retry refusal on Node 24 and Node 20. |
| WEB-R9 | VERIFIED | A synchronous credential provider can exhaust the connector deadline before dispatch while the timer callback is starved, yet the request is still sent. | Independent rereview confirms timeout with `request_may_have_been_sent:false` and zero fetch calls on Node 24 and Node 20. |
| WEB-R10 | VERIFIED | Connector response timeout does not cancel a hostile reader or zero buffered chunks on every completion path. | Independent rereview proves one bounded cancellation attempt and zeroed retained bytes on Node 24 and Node 20. |
| WEB-R11 | VERIFIED | Credential redaction misses common OTP, TOTP, PIN, and verification-code carrier names. | Independent rereview proves query, header, JSON, and form carrier values do not persist on Node 24 and Node 20. |
| WEB-R12 | VERIFIED | Chrome rejects an oversized declared response before acquiring and cancelling its body reader, leaving the producer active. | Independent rereview proves reader acquisition and bounded cancellation before refusal on Node 24 and Node 20. |
| WEB-R13 | VERIFIED | Injected fetch and authorization replay can retain a byte chunk that resolves after timeout. | Independent rereview proves late and retained chunks are zeroed on Node 24 and Node 20. |
| WEB-R14 | VERIFIED | Secondary credential headers could survive capture, and role credentials could inject routing or method-override headers that change the authorized destination or action. | Independent rereview covers fixed sensitive names plus the `x-forwarded-*`, `x-original-*`, and `x-rewrite-*` families on Node 24 and Node 20. |
| WEB-R15 | VERIFIED | Synchronous response metadata, header iteration, body finalization, and durable settlement could exhaust an absolute deadline while starving its timer and still return success. | Independent replay, Chrome, injected-fetch, and generated-runtime deadline regressions all return timeout after the blocking callback on Node 24 and Node 20. |
| WEB-R16 | VERIFIED | Injected and generated connectors could collect unbounded response headers or return credential-bearing response headers and concrete redirect query data. | Independent rereview confirms bounded header collection and value-redacted result metadata on Node 24 and Node 20. |
| WEB-R17 | VERIFIED | Scan, planning, controller, and report paths trusted legacy persisted request bundles before replay-time sanitization and could re-persist or surface raw credentials. | Independent legacy load, planning, scan, replay, and report regressions pass on Node 24 and Node 20. |
| WEB-R18 | VERIFIED | Generated connectors retained their internal outgoing byte-body clone after the transport settled. | Independent rereview proves the transport-visible clone is zeroed while the caller-owned buffer remains unchanged on Node 24 and Node 20. |
| WEB-R19 | VERIFIED | Generated connectors accepted delimiter-bearing path replacements and allowed observed read-action query or body inputs to be substituted with state-changing actions. | Independent rereview proves raw/layered path delimiters and query/body action drift fail before fetch on Node 24 and Node 20. |
| WEB-R20 | VERIFIED | A generated connector response using the `arrayBuffer()` fallback could resolve after its deadline and retain the abandoned response bytes. | Independent rereview proves a late fallback buffer is zeroed after bounded timeout failure on Node 24 and Node 20. |
| WEB-R21 | VERIFIED | Generated connectors treated any two unclassified semantic action values as equivalent `OTHER_ACTION`, allowing request or redirect action substitution without proving equivalent behavior. | Independent rereview proves caller and redirect `OTHER_ACTION` drift fail before the affected dispatch on Node 24 and Node 20. |
| WEB-R22 | VERIFIED | Unkeyed numeric/boolean JSON values and malformed form percent encodings could be persisted as credential-free request material. | Independent integration review confirms top-level and array primitives are replaced and malformed form bodies are omitted before persistence. Layered field-name encoding is separately tracked as WEB-R28. |
| WEB-R23 | VERIFIED | Authorization replay normalized a response header collection without count, per-value, or aggregate byte bounds. | Independent integration review confirms the bounded header iterator and durable `THREW` settlement regression. |
| WEB-R24 | VERIFIED | A legacy finding's raw tester-role value reached the report filename and could inject path separators or platform-invalid filename characters. | Independent integration review confirms stable ordinal filenames and coverage for mixed separators and punctuation. |
| WEB-R25 | VERIFIED | Generated connectors did not bind substituted path segments or action values nested in arrays to observed action classes, including redirect paths. | Independent integration review confirms substituted paths and nested-array action drift are refused before the affected fetch. |
| WEB-R26 | VERIFIED | Omitted or empty semantic action fields in query, body, or redirects inherited the endpoint's observed read/retry classification. | Independent integration review confirms classified values are required for every observed semantic action field, including omitted fields and empty action arrays. |
| WEB-R27 | VERIFIED | HAR and generated-runtime credential filtering relied on a fixed header-name list, so custom token/signature headers could be misclassified or returned to callers. | Independent integration review confirms dynamic token/signature carriers and historical 1.0 compatibility. Layered names and remaining SSO classifier parity are tracked as WEB-R28 and WEB-R29. |
| WEB-R28 | VERIFIED | Layered encoded field names bypassed capture redaction and generated response-header credential filtering. | Independent rereview confirms both classifiers perform at most eight decode passes and fail closed on unresolved or malformed names; capture and generated-result regressions pass on Node 24 and Node 20. |
| WEB-R29 | VERIFIED | The generated response-header classifier omitted SSO names already recognized by capture redaction, including RelayState, SAMLRequest, SAMLResponse, and state. | Independent rereview confirms SSO carrier parity and mechanically projects every exported canonical capture header through the generated response filter on Node 24 and Node 20. |
| WEB-R30 | VERIFIED | Generated connector timer setup occurred after cloning an outgoing byte body but before cleanup ownership, while throwing timer cleanup replaced success or timeout and skipped private byte erasure. | Timer setup is inside cleanup ownership, setup failure is typed before send, and cleanup cannot replace the request outcome or prevent byte erasure; setup, success, and timeout regressions pass on Node 24 and Node 20. Independent Node 24.13.0 and portable Node 20.20.2 post-fix review verifies this row. |
| WEB-R31 | VERIFIED | Page-injected fetch trusted target-controlled timer globals, so setup failure left stale bridge abort state and throwing cleanup replaced success or timeout while retaining a transient session header reference. | The deadline rejection is observed before setup, setup failure clears bridge state, and best-effort timer cleanup preserves result and transient-header cleanup; deterministic setup, success, and timeout regressions pass on Node 24 and Node 20. Independent Node 24.13.0 and portable Node 20.20.2 post-fix review verifies this row. |
| WEB-R32 | VERIFIED | A target-controlled timer that fired synchronously before the injected fetch boundary was reported as a post-dispatch timeout even though `fetchImpl` was never invoked. | The injected route now binds `request_may_have_been_sent` to the exact `fetchImpl` invocation boundary: a pre-invocation deadline remains a typed unsent timeout, while failure after invocation remains delivery-ambiguous. Deterministic pre- and post-invocation regressions pass on Node 24 and Node 20. Independent Node 24.13.0 and portable Node 20.20.2 post-fix review verifies this row. |
| WEB-R33 | VERIFIED | Generated connectors cloned nested caller and credential byte values before registering cleanup ownership, so a later clone or credential-shape failure retained the earlier private byte copies. | One request-scoped ownership set now records every byte clone at creation and erases each clone best-effort across shape, provider, timer, and transport exits; deterministic partial caller-clone and invalid nested credential regressions pass on Node 24 and Node 20. Independent Node 24.13.0 and portable Node 20.20.2 post-fix review verifies this row. |
| WEB-R34 | VERIFIED | Generated connectors cloned a raw caller body before entering cleanup ownership, so a throwing ambient monotonic clock leaked the private clone and an untyped exception before dispatch. | Cleanup ownership now begins before the first body clone and covers every subsequent setup step; raw setup failures are typed before send and every tracked clone is erased best-effort. The deterministic clock-failure regression passes on Node 24 and Node 20. Independent Node 24.13.0 and portable Node 20.20.2 post-fix review verifies this row. |
| WEB-R35 | VERIFIED | Vendor- and namespace-prefixed SSO response headers bypassed suffix classification, allowing OAuth state, SAML request/response, relay state, and authorization-code values into generated connector results. | Capture and generated-result classifiers share bounded-version suffix handling for `state`, `relaystate`, `samlrequest`, and `samlresponse`; `code` is redacted only as an exact carrier or when an auth, security, identity, or vendor context precedes it, preserving ordinary status, response, and error-code metadata. Both classifiers retain bounded layered decoding and fail closed on malformed, unresolved, or excessive version suffixes. Deterministic capture and generated-result regressions pass on Node 24 and Node 20. Independent Node 24.13.0 and portable Node 20.20.2 post-fix review verifies this row. |
| WEB-R36 | VERIFIED | Page-injected fetch assigned its bridge abort controller before reading a target-controlled monotonic clock, so a throwing clock left a live stale controller and transient session carrier after an untyped pre-dispatch failure. | One outer cleanup boundary now owns the exact bridge controller, transient carrier, body bytes, fetch lookup, clock setup, and timer setup; a throwing clock returns a typed unsent setup failure and conditionally clears only its own controller. The deterministic stale-state regression passes on Node 24 and Node 20. Independent Node 24.13.0 and portable Node 20.20.2 post-fix review verifies this row. |
| WEB-R37 | VERIFIED | Page-injected Fetch shared its decoded request bytes with target code, so target detachment made fetch-error and final cleanup throw, replacing the primary outcome and risking stale bridge state. | Every injected byte cleanup uses a captured no-throw intrinsic inside the existing state/header/controller boundary; target detachment on both failure and success preserves the typed or successful result and clears bridge state on Node 24 and Node 20. Independent Node 24.13.0 and portable Node 20.20.2 post-fix review verifies this row. |
| WEB-R38 | VERIFIED | Generated response reading created a retained copy before producer cleanup but registered it afterward, while producer and late-response wipes could throw or reject unhandled on overridden or detached storage. | Retained copies enter private tracking before producer scrubbing, every producer and owned-copy wipe uses a no-throw intrinsic, and late cleanup handlers cannot reject; hostile-fill and late-detached-buffer regressions pass on Node 24 and Node 20. Independent Node 24.13.0 and portable Node 20.20.2 post-fix review verifies this row. |
| WEB-R39 | VERIFIED | The Chrome companion retained controller response chunks whose cleanup could throw after a producer override or later detachment, and its late-read cleanup could reject unhandled. | Controller response and late-read byte erasure now use a no-throw intrinsic while private trackers always clear; deterministic hostile-fill and late-detached-read regressions pass on Node 24 and Node 20. Independent Node 24.13.0 and portable Node 20.20.2 post-fix review verifies this row. |
| WEB-R40 | VERIFIED | A target fetch could freeze its session-bearing request-header object, making inner carrier deletion throw and replace either a successful response or the true post-dispatch failure with a raw error and false delivery classification. | Session-carrier and exact bridge-controller ownership now settle only through the outer no-throw cleanup boundary; deterministic frozen-header success and thrown-fetch regressions preserve the successful result or typed sent failure on Node 24 and Node 20. Independent Node 24.13.0 and portable Node 20.20.2 post-fix review verifies this row. |
| WEB-R41 | VERIFIED | Exceptions from target-controlled response metadata or finalization after `fetch` returned escaped through the browser-dispatch projector as unsent injection failures; the projector also trusted hostile error-property getters, and a throwing getter on a late reader result could reject cleanup unhandled. | Fetch invocation state now lives in the outer outcome scope, error metadata is read defensively, every untyped post-invocation exception projects as a typed sent fetch failure, and late reader fulfillment cleanup catches hostile value access. Final-clock, hostile-header/error-getter, and late-value-getter regressions pass on Node 24 and Node 20. Independent Node 24.13.0 and portable Node 20.20.2 post-fix review verifies this row. |
| ENG-R1 | VERIFIED | The natural-language full-profile parser rejected ordinary authorization without a magic scope word and accepted negated, denied, and restricted authority statements. | Independent adversarial review confirms ordinary target authority is accepted while explicit lack, denial, restrictions, and exclusions contradict `full`; expanded statement regressions pass on Node 24 and Node 20. Follow-up grammar gaps are tracked as ENG-R16. |
| ENG-R2 | VERIFIED | `engage full` scheduled only 11 routes and omitted public proof, adversarial, bounty, acquisition, provider/remote, transparency, database, and evidence routes. | Independent review confirms registry v2 enumerates all 29 public controller families: 11 enabled routes have fixed immutable invocations, while 18 unavailable routes have explicit reason codes, null invocations, durable waiting state, and no dispatch path. |
| ENG-R3 | VERIFIED | The canonical skill said `full` selected an evidence route although no such engagement route existed. | Independent schema, registry, generated-inventory, and package review confirms the SHA-256 marker binds the canonical skill to the exact ordered route and capability projection; drift regressions pass on Node 24 and Node 20. |
| ENG-R4 | VERIFIED | Accepted `vault:` and `env:` credential references were sealed but inert because only browser references resolve into route material. | Independent creation and persisted-state review confirms schemes without an executable adapter are rejected; browser and injectable browser-session references remain supported, and tampered persisted references fail closed. |
| ENG-R5 | VERIFIED | Capability inventory advertises `http-recon` and `http-authed` commands that a packaged install does not expose as bins or root subcommands. | Independent root-help and packaged-command review confirms every advertised controller family dispatches through the installed `last-aperture` bin; release-wiring tests pass on Node 24 and Node 20. |
| ENG-R6 | VERIFIED | `doctor` advertises reverse/Burp/unified availability without checking Ghidra, Java, Frida, Burp, Crane, or engagement workflow readiness. | Independent review confirms static host-tool checks and workflow projections stay inert, distinguish unchecked runtime identity, and fail closed for unavailable or unusable prerequisites. |
| ENG-R7 | VERIFIED | Fresh npm installs omit `package-lock.json` and hoist dependency metadata, so every doctor workflow reports missing prerequisites in a valid installed package. | The synchronized shrinkwrap ships in the 819-entry tarball; a fresh install resolves all four hoisted direct dependencies and reports source review `STATIC_CHECKS_PASSED`; malformed-lock coverage is tracked separately as ENG-R13. |
| ENG-R8 | VERIFIED | Engagement, Ghidra, and Frida workflows report static readiness when required shipped entrypoints, schemas, exporters, or fixed agents are absent. | Independent deletion and package-inventory checks cover every direct route asset; absent, empty, linked, or unpackaged assets cannot satisfy the affected workflow. |
| ENG-R9 | VERIFIED | Doctor reports OCI registry acquisition as statically ready even though the public acquisition controller rejects every route and the capability registry declares it unavailable. | Independent installed-package inspection confirms Crane remains observable while `oci-registry-acquisition` is `UNAVAILABLE` with `oci-registry-acquisition-controller` as its blocker. |
| ENG-R10 | VERIFIED | Doctor requires the Windows Ghidra Java compatibility toolchain on every platform and launcher shape. | Independent cross-platform launcher-shape regressions confirm JDK and compatibility-agent checks are applicable only to Win32 `.bat`/`.cmd`; runtime-selection binding is tracked separately as ENG-R14. |
| ENG-R11 | VERIFIED | Root doctor opens an inspection bundle before fully validating worker and host-tool paths. | Shared lexical normalization validates every operator endpoint before bundle I/O; independent malformed-path ordering regression passes on Node 24 and Node 20. |
| ENG-R12 | VERIFIED | Doctor reports a hard-linked route tool or shipped route asset as ready even though the corresponding runtime controller rejects it. | Independent hard-link and read-identity review confirms single-link endpoints and link count remain bound before and after bounded reads; focused suites pass on Node 24 and Node 20. |
| ENG-R13 | VERIFIED | Doctor accepted any existing `npm-shrinkwrap.json`, including `{}`, as a publishable dependency lock and ignored a valid source lockfile only after claiming readiness. | Independent source and installed-package review confirms the preferred shrinkwrap is bounded-parsed and must match package name/version, exact direct dependency pins, and each direct package record; malformed-lock and hoisted-install regressions pass on Node 24 and Node 20. |
| ENG-R14 | VERIFIED | Arbitrary regular files supplied as `--javac` and `--jar` could make Windows batch Ghidra look statically ready even though runtime discovery would select different endpoints; irrelevant JDK checks also appeared missing on native launchers. | Independent review confirms doctor and runtime share the Windows candidate generator, supplied paths must equal the first usable runtime candidate, and non-batch checks are `NOT_APPLICABLE`; deterministic regressions pass on both supported Node versions. |
| ENG-R15 | VERIFIED | On POSIX hosts a regular but non-executable Ghidra, Frida, Crane, Docker, or archive path could satisfy static workflow prerequisites. | Independent review confirms native executables require host execute access and identity is re-bound afterward; POSIX permission regressions pass on Node 24 and Node 20. |
| ENG-R16 | VERIFIED | Follow-up authority parsing still accepted reported, questioned, disavowed, and conditional authorization, while treating `no changes to scope` as a restriction. | Independent adversarial rereview added red-first third-party `told`/`confirmed`/quoted reports, punctuation-free questions, claim rejection, and `once`/`when`/`upon` approval conditions. Only a clause-delimited scope-continuity note is exempted, so `authorized to make no changes to scope` remains restrictive; ordinary direct assertions remain accepted on Node 24 and Node 20. |
| ENG-R17 | VERIFIED | A profile could be sealed for a target kind with no compatible registered route, leaving a valid engagement permanently active with zero executable or waiting work. | Independent review mechanically compares all five authorization profiles with all six target kinds against the immutable canonical registry and confirms every zero-route pairing is rejected on Node 24 and Node 20. |
| ENG-R18 | VERIFIED | Resume revalidated process and device targets but did not revalidate repository or artifact targets, so a durable local target could become a link or reparse alias without failing closed. | Independent review found same-path regular-object replacement remained possible. Repository and artifact route plans now bind a filesystem-instance SHA-256 over device, inode, and birth time, while artifact plans also retain their content digest; deterministic link-transition, real replacement-directory, and identical-byte replacement-artifact regressions return `ENGAGEMENT_TARGET_CHANGED` on Node 24 and Node 20. |

## Platform, CI, packaging, and installation

| ID | Status | Finding | Primary code | Required proof |
| --- | --- | --- | --- | --- |
| PLAT-01 | VERIFIED | Validate Windows Ghidra helper paths using Windows semantics on every host platform. | `scripts/lib/reverse-ghidra.mjs` | Local Windows regressions pass on Node 20 and Node 24; the cross-host Windows launcher-policy regression also ran and passed on hosted Linux under Node 20.20.0 and Node 24.20.0 in Actions run `34715712032`. |
| PLAT-02 | VERIFIED | The doctor regression supplied a `.bat` file as a POSIX launcher without execute permission, so the executable prerequisite correctly blocked before the test could prove Windows-only JDK and compatibility-agent behavior. | `scripts/lib/doctor.mjs`, `test/doctor.test.mjs` | Windows batch classification now explicitly uses Windows path semantics, and the POSIX fixture has execute permission while asserting the executable passes and the JDK/agent checks remain inapplicable. The regression runs and passes on hosted Linux under Node 24.20.0 and Node 20.20.2 in Actions run `34718095126`. |
| CI-01 | VERIFIED | Run the supported matrix for pull requests, with concurrency and least required permissions. | `.github/workflows/lint-lenses.yml` | Actions run `34718095126` passes the complete test suite under Node 24.20.0 and Node 20.20.2 for PR `#9` head `c963eb7c4b3c8d93635d12614767d722cb198e3f`; the Node 24 job also passes lint and the topic, benchmark, and capability drift checks. |
| PKG-R1 | VERIFIED | Python bytecode caches could enter the npm archive and disclose exact local source paths. | `package.json`, `.npmignore`, `test/reverse-release-wiring.test.mjs` | A synthetic path-bearing bytecode fixture is excluded during pack; the independently inspected 817-file archive contains no cache or bytecode entry and no absolute workstation path. |
| PKG-R2 | VERIFIED | Packaging the live remediation ledger would make the release artifact describe and eventually digest itself. | `package.json`, `.npmignore` | The remediation directory is excluded from the npm archive while the public changelog and release documentation remain present. |
| PKG-R3 | VERIFIED | The shipped package needed an exact target-neutral residue check rather than relying on repository-wide historical material. | `scripts/scan-residue.mjs`, package inventory | The independently byte-scanned package contains zero configured customer, tenant, or product residue terms; historical planning material is excluded from the archive. |
| PKG-R4 | VERIFIED | A shipped synthetic test fixture retained the local workstation user name in an absolute Windows path. | `test/existence-ingest.test.mjs` | The fixture now uses a generic operator path; its focused regression passes and the rebuilt package byte scan contains no local workstation path or user identity. |
| REL-01 | VERIFIED | Bump the package version and build a uniquely named uploadable artifact after all fixes. | `package.json`, version module, release artifact | `last-aperture-0.14.1.tgz` contains 817 files, 3,588,981 packed bytes, and 14,056,987 unpacked bytes; SHA-256 `17319e04d6467e4eeafbe321dd62139a3f4287ecbb737a515002fb3fbea9f68d`. A clean offline install resolves every exact direct pin, exposes the installed root CLI, reports all 32 capabilities, and passes doctor and required-asset smoke checks. |
| REL-02 | VERIFIED | Public protocol, ADR, and browser-security pages still labeled 0.13.0 as the current platform after the 0.14.1 bump, and the historical test-execution proposal contradicted the current narrow T1/T2 routes. | Current protocol, active-route, and packaged-companion labels now name 0.14.1; the superseded proposal records its 0.13 state as historical and points to the current narrow proof status. Release wiring binds the companion claim to the release version; Independent Node 24.13.0 and portable Node 20.20.2 post-fix review verifies this row. |
| INST-01 | VERIFIED | Ensure the Codex and Claude skill installations resolve to the repaired package without duplicate skill discovery. | local skill links/layout | Both junctions target only `skills/last-aperture`, expose one `SKILL.md`, hash-match the working tree, and resolve the controller root. |
| RUN-01 | VERIFIED | Execute a real Ghidra headless analysis through Last Aperture. | local Ghidra 12.1.2 | Completed with 106 observations against a copied Windows binary. |
| RUN-02 | VERIFIED | Install and validate a working Frida CLI/runtime. | Frida 17.18.0, frida-tools 14.10.4 | Last Aperture v1/v2 local-spawn fixture traces succeeded with 202/203 observations. |
| RUN-03 | VERIFIED | Install/find Burp Suite and compile/load the Montoya extension against the real API. | Burp 2026.8-53343 and persistent extension JAR | Official JAR checksum, launcher, real-API compile, export/import conformance, and isolated live initialization all pass. |
| RUN-04 | VERIFIED | Record Docker/crane readiness for OCI live validation. | local environment | Crane 0.22.1 resolved and fetched an exact public multi-platform OCI manifest without a Docker daemon. |
| RUN-05 | VERIFIED | Load the unpacked Chrome companion and exercise its real Manifest V3 registration/runtime boundary. | Chrome 153.0.8010.36 isolated profile | DevTools loaded exact repository v0.14.1 as extension `nhjkeknnbnpbpibjgclmeocficiiofdc`; its worker activated, popup-to-worker `STATUS` returned `IDLE`, and no worker or popup exception was observed. Headless optional-loopback permission prevented a synthetic dispatch, so this row proves registration/runtime only. |

## Validation log

| Date/time (Kyiv) | Scope | Command/evidence | Result |
| --- | --- | --- | --- |
| 2026-09-12 baseline | Full Node suite | `npm.cmd test` | FAIL: 1 stop-marker race; 2 skipped. |
| 2026-09-12 baseline | Focused review suite | targeted Node tests | PASS: 808; 2 skipped. |
| 2026-09-12 baseline | Static/generated checks | lint, generators, benchmark/capability generation | PASS. |
| 2026-09-12 baseline | Python bounty conformance | Python test suite | PASS: 38/38. |
| 2026-09-12 baseline | Dependency audit | `npm audit --offline` | PASS: zero vulnerabilities. |
| 2026-09-12 remediation | Pull-request CI wiring | `node --test test/release-wiring.test.mjs` | PASS: 18/18; commit `edf9035`. |
| 2026-09-12 remediation | Frida runtime | Frida 17.18.0 / tools 14.10.4, Last Aperture v1 and v2 local spawn | PASS: `SUCCEEDED`, 202 and 203 observations. |
| 2026-09-12 remediation | Burp runtime and Montoya | Burp 2026.8-53343; desktop JAR SHA-256 `888f0588cf82a3b70d508dcbf07591184a26249e38cda8ea8b5a68f4b5a30789`; `verify.ps1` | PASS: launcher, exact checksum, real-API compile, mock export, HAR core import. Persistent extension SHA-256 `ad5d1c5bddaa7646e7fbbdce0001f2c3c96c0f695b398149a1fb51f76ee4b8b7`. |
| 2026-09-12 remediation | Burp live extension load | Isolated Community startup with a disposable wrapper that records phases only after delegating to `LastApertureBurpExtension.initialize`; evidence `C:\Users\geral\Red Team\scratchpad\burp-live-20260912-probe4\extension-phases.txt`, SHA-256 `6abe0b1731def487983c5b9a4cf5d256e8374f7189957879e3cf0f6d64f77286` | PASS: `CLASS_LOADED`, `INITIALIZE_ENTERED`, `DELEGATE_RETURNED`; Burp reported `Burp Suite Community Edition 2026.8-53343`; process stopped after proof. |
| 2026-09-12 remediation | Codex and Claude skill installation | Canonical junction inventory, SHA-256 comparison, controller-root resolution, `capabilities --json`, skill/release wiring tests | PASS: each catalog link exposes one skill; all three skill files hash to `c69754cbb2815476709b5ba217f3a362c73628223a054f5d7d4fb97bcca2134b`; controller command succeeds; 41/41 tests pass. |
| 2026-09-12 remediation | Crane OCI runtime | Crane 0.22.1 SHA-256 `e22e47202cece45d09fe92919bdb683f56f548d360cb96f6a852defd965c935d`; `crane digest registry.k8s.io/pause:3.10`; exact manifest fetch at `sha256:ee6521f290b2168b6e0935a181d4cff9be1ac3f505666ef0e3c98fae8199917a` | PASS: public digest resolved and its OCI manifest list fetched without Docker daemon. Docker 29.5.3 remains installed with its daemon stopped. |
| 2026-09-12 remediation | Final package-surface audit | `npm pack --dry-run --json --ignore-scripts`; archive inventory and byte scan; synthetic path-bearing Python-bytecode fixture in the release-wiring test | PASS: version 0.14.1 has 817 entries; changelog, shrinkwrap, Ghidra, Frida, Burp, Chrome, controllers, skills, and bins are present; remediation/history/scratch/cache/bytecode entries are absent; zero absolute workstation paths or configured target-specific residue terms were found. |
| 2026-09-12 remediation | Unpacked Chrome registration/runtime | Isolated Chrome 153.0.8010.36 profile loaded the repository extension through DevTools `Extensions.loadUnpacked`; evidence `C:\Users\geral\Red Team\scratchpad\chrome-live-0.14.1-20260912T204135478\validation-summary.json`, SHA-256 `ed05afd6f39ac646f86c4ef2bd6ef8345baae5fa91349a45183f7f4716b2e458` | PASS: exact v0.14.1 path/ID, enabled state, worker activation, popup rendering, and popup-to-worker `IDLE` status; zero worker/popup exceptions. Headless optional-loopback permission prevented synthetic dispatch, so no dispatch claim is made. |
| 2026-09-12 remediation | Supported Node floor | Portable official Node 20.20.2 archive SHA-256 `dc3700fdd57a63eedb8fd7e3c7baaa32e6a740a1b904167ff4204bc68ed8bf77`, matched against Node's `SHASUMS256.txt`; Ghidra test plus skill/release wiring tests | PASS on Node 20: 18/18 Ghidra and 41/41 skill/release wiring; no failures or cancellations. |
| 2026-09-12 remediation | Unified installed commands and static toolchain doctor | Root help smoke for engagement, HTTP recon/auth, reverse, adversarial, bounty, acquisition, and database; doctor/capability/release suites; exact installed Ghidra/JDK/Frida/Burp/Crane paths | PASS: 21/21 focused tests; all seven tool checks `PASS`; all six corresponding workflow projections `STATIC_CHECKS_PASSED`. Independent review pending. |
| 2026-09-12 remediation | Engagement authority and credential intake | Ordinary/negated/restricted statement matrix and supported credential-reference creation tests on Node 24.13.0 and portable Node 20.20.2; full engagement suite on Node 24 | PASS: 18/18 focused tests on each Node version; full engagement suite and lint pass. Independent review pending. |
| 2026-09-12 remediation | Full-profile route inventory and second-pass authority hardening | Node 24.13.0 and portable Node 20.20.2 authorization, browser-material, profile-inventory, route-registry, CLI/release-wiring, capability, target, and controller regressions; `npm.cmd run lint`; `node scripts/gen-capabilities.mjs --check`; `git diff --check` | PASS: combined focused suites 73/73 on each Node version; full engagement controller 27/27 on Node 24 and selected start/repository lifecycle 2/2 on Node 20; skill 7,836/8,000 bytes; generated documentation and diff checks clean. Independent review pending. |
| 2026-09-12 remediation | Browser, HAR, bounty, and connector fixes | Sixteen focused browser/HAR/bounty/connector test files on Node 24.13.0 and checksum-verified portable Node 20.20.2; 15 scoped production-file syntax checks; lint; diff check | PASS: 287/287 on each Node version with zero failures, skips, or cancellations; generated connector 24/24; Chrome extension 19/19; injected fetch 18/18; all syntax checks, `npm.cmd run lint`, and `git diff --check` pass. |
| 2026-09-12 remediation | Independent final browser, HAR, bounty, and generated-connector rereview | Expanded nineteen-file matrix adds authorization response normalization, legacy report drafting, and browser bridge coverage on Node 24.13.0 and checksum-verified portable Node 20.20.2; 15 scoped syntax checks; lint; all three generator checks; diff check | PASS: 331/331 on each Node version with zero failures, skips, or cancellations; reverse HAR/protocol/generated connector 66/66; syntax 15/15; lint and generators pass. Independent rereview found and fixed WEB-R22 through WEB-R27. |
| 2026-09-12 remediation | Final independent HTTP campaign/recon lifecycle rereview | Six focused suites on Node 24.13.0 and checksum-verified portable Node 20.20.2; ten repetitions of 24-way stale-reclaim stress; twenty repetitions of the Node-20 Win32 owner-publication/release race; eight JavaScript and one PowerShell syntax checks; lint and diff checks | PASS: 229/229 on each Node version with zero failures, skips, or cancellations; all ten stale-reclaim and twenty Win32 publication/release repetitions pass. Independent rereview verified HTTP-R1 through HTTP-R16 and found/fixed HTTP-R17 and HTTP-R18. |
| 2026-09-12 remediation | Unified doctor and package-install review fixes | Doctor, capability, and engagement-release regressions on Node 24.13.0 and portable Node 20.20.2; npm pack inventory; real temporary tarball install; generated-capability, lint, syntax, and diff checks | PASS: 26/26 on each Node version; all required assets and publishable shrinkwrap ship; installed doctor resolves all four hoisted direct dependencies and reports source review `STATIC_CHECKS_PASSED`; OCI acquisition remains `UNAVAILABLE`. Independent post-fix review pending. |
| 2026-09-12 remediation | Evidence acquisition, bundle, adapters, OCI, canonical ordering, and Ghidra | Node 24.13.0: `node --test test/evidence-acquire-controller.test.mjs test/evidence-bundle.test.mjs test/evidence-cli-runner.test.mjs test/evidence-image-reference.test.mjs test/evidence-registry-adapter.test.mjs test/evidence-deployed-adapter.test.mjs test/evidence-runtime-adapter.test.mjs test/oci-normalizer.test.mjs test/canonical-ordering.test.mjs test/reverse-ghidra.test.mjs`; `git diff --check` | PASS: 211/211; zero failures, skips, or cancellations; canonical ordering 9/9; diff check clean. |
| 2026-09-12 remediation | Evidence/OCI supported Node floor | Portable official Node 20.20.2: the same 11-suite command with the checksum-verified `node.exe` | PASS: 211/211; zero failures, skips, or cancellations. |
| 2026-09-12 remediation | Broad Evidence/OCI regression sweep | Node 24.13.0: every `test/evidence-*.test.mjs` plus OCI normalizer, canonical ordering, and Ghidra | PASS: 447/447; zero failures, skips, or cancellations. |
| 2026-09-12 remediation | Whole repository after Evidence/OCI fixes | `npm.cmd test` on Node 24.13.0 | FAIL outside this ownership: concurrent engagement full-profile/schema edits reject generated route capabilities/effects; all reached Evidence/OCI tests pass. |
| 2026-09-12 remediation | Independent doctor and package-install rereview | Node 24.13.0 and checksum-verified portable Node 20.20.2: doctor, capability, filesystem-endpoint, engagement/reverse/release-wiring, and Ghidra suites; npm dry-run and real pack; fresh tarball install; installed root-bin smoke; lint; all three generator checks; syntax and diff checks | PASS: 76/76 on each Node version with zero failures, skips, or cancellations. Review tarball contains 819 entries (3,564,648 bytes); 16 direct readiness assets were mechanically confirmed, the installed root bin dispatches controller help, all four hoisted dependencies and the structural shrinkwrap pass, source review is `STATIC_CHECKS_PASSED`, and OCI remains `UNAVAILABLE`. Independent review also found and fixed ENG-R13 through ENG-R15. |
| 2026-09-12 remediation | Independent engagement, full-profile, doctor, and installed-package rereview | Node 24.13.0 and checksum-verified portable Node 20.20.2: twelve engagement/doctor/capability/skill/release suites; isolated npm pack and registry-backed install; installed root-bin help and doctor; all three generator checks; lint; syntax and diff checks | PASS: 146/146 on each Node version with zero failures, skips, or cancellations. Review tarball contains 819 entries (3,575,500 bytes); the installed package resolves exact direct pins `acorn@8.15.0`, `ajv@8.20.0`, `fast-check@4.9.0`, and `yaml@2.9.0`, ships shrinkwrap v3 and the canonical skill, reports source review `STATIC_CHECKS_PASSED`, and keeps OCI registry acquisition `UNAVAILABLE`. Independent rereview verified ENG-R1 through ENG-R4 and ENG-R13 through ENG-R15, and found/fixed ENG-R16 through ENG-R18. |
| 2026-09-12 remediation | Final independent ENG-R16 through ENG-R18 rereview | Node 24.13.0 and checksum-verified portable Node 20.20.2: authorization, controller, target, CLI, profile-inventory, route-registry, engagement-release, skill, and release-wiring suites; four scoped syntax checks; lint; all three generator checks; diff check | PASS: 113/113 on each Node version with zero failures, skips, or cancellations. Independent red-first review expanded the authority grammar boundary, made the profile/target proof exhaustive, and found/fixed same-path repository and identical-byte artifact replacement on resume; ENG-R16 through ENG-R18 are verified. |

| 2026-09-12 remediation | Final independent browser/HTTP/engagement integration review | Thirty-four focused browser, HAR, bounty, connector, HTTP campaign/recon, engagement, doctor, capability, skill, and release suites on Node 24.13.0 and portable Node 20.20.2; four production syntax checks; lint; all three generator checks; package dry-run inventory; diff check | PASS: Node 24 644/644 plus separately invoked skill suite 23/23, Node 20 667/667; zero failures, skips, or cancellations. Ten required runtime assets are present in the 819-entry, 3,591,505-byte review package inventory. Independent review verifies HTTP-R17/R18 and WEB-R22 through WEB-R27, and finds/fixes HTTP-R19 through HTTP-R21 plus WEB-R28/R29 with synthetic red-first regressions. New fixes await independent post-fix review. |
| 2026-09-12 remediation | Independent HTTP-R19 through HTTP-R21 and WEB-R28/WEB-R29 rereview | HTTP client, mutation controller, authorization-request redaction, and generated-connector suites on Node 24.13.0 and checksum-verified portable Node 20.20.2; eight scoped syntax checks; lint; all three generator drift checks; diff check | PASS: 114/114 on each Node version with zero failures, skips, or cancellations; fixed header parity is derived mechanically from the canonical capture inventory. Independent rereview verifies the five assigned rows and finds/fixes HTTP-R22/R23 with deterministic red-first transport-copy regressions. |
| 2026-09-12 remediation | Final independent Evidence/OCI rereview and remediation | Thirteen focused Evidence/OCI/process suites and every `test/evidence-*.test.mjs` plus OCI normalizer, canonical ordering, Ghidra, and process suites on Node 24.13.0 and checksum-verified portable Node 20.20.2; 85 modified JavaScript syntax checks; Windows helper parse; lint; all three generator drift checks; diff check | PASS: focused 296 total, 293 pass, 3 POSIX-only skips on each runtime; broad 499 total, 496 pass, 3 POSIX-only skips on each runtime; zero failures, cancellations, or todos. Lint reports 22 lenses/243 slugs; topics, 84 benchmark cases, and capability documentation are in sync; PowerShell parse and `git diff --check` pass. Independent rereview verifies BASE-R4, EVID-01 through EVID-09, EVID-R1 through EVID-R13, and OCI-01 through OCI-04; new EVID-R14 through EVID-R24 and OCI-05 through OCI-12 fixes await independent post-fix review. |
| 2026-09-12 remediation | Evidence/OCI post-fix independent verification | Eight late-delta suites and the broad Evidence/OCI, canonical-ordering, Ghidra, and process matrix on Node 24.13.0 and checksum-verified portable Node 20.20.2; 14 scoped syntax checks; Windows helper parse; lint; all three generator drift checks; ledger tests; diff check | PASS: late-delta 218 total, 215 pass, 3 POSIX-only skips on each runtime; broad 499 total, 496 pass, 3 POSIX-only skips on each runtime; zero failures or cancellations. EVID-R14 through EVID-R17, EVID-R19 through EVID-R24, and OCI-05 through OCI-12 are independently verified. EVID-R18 source and deterministic process-group tests pass, while its live self-detached POSIX cases remain skipped on Windows pending a Linux Node run. |
| 2026-09-12 remediation | Independent HTTP-R22/R23 and final browser ownership rereview | Nine focused native HTTP, browser bridge/extension/injected, campaign, mutation, authorization-redaction, transport-boundary, and generated-connector suites on Node 24.13.0 and checksum-verified portable Node 20.20.2; eight scoped syntax checks; lint; all three generator drift checks; diff check | PASS: 204/204 on each Node version with zero failures, skips, or cancellations. Independent rereview verifies HTTP-R22/R23, reconfirms HTTP-R19 through HTTP-R21 and WEB-R28/WEB-R29, and finds/fixes HTTP-R24 through HTTP-R28 with deterministic red-first observer ownership and timer-boundary regressions. New fixes await independent post-fix review. |
| 2026-09-12 remediation | Independent HTTP-R24 through HTTP-R28 and web-classifier post-fix rereview | The same nine-suite native HTTP, browser, campaign, mutation, authorization-redaction, transport-boundary, and generated-connector matrix on Node 24.13.0 and checksum-verified portable Node 20.20.2; eight scoped syntax checks; ledger tests; lint; all three generator drift checks; diff check | PASS: 206/206 on each Node version with zero failures, skips, or cancellations; ledger tests 10/10. Independent rereview verifies HTTP-R24 through HTTP-R28 and reconfirms WEB-R28/WEB-R29, then finds/fixes HTTP-R29/R30 with deterministic red-first duplicate-result and stale-timer regressions. New fixes await independent post-fix review. |
| 2026-09-12 remediation | Final independent HTTP-R29/R30 race verification | The same nine-suite native HTTP, browser, campaign, mutation, authorization-redaction, transport-boundary, and generated-connector matrix on Node 24.13.0 and checksum-verified portable Node 20.20.2; scoped syntax, ledger, lint, generator-drift, and diff checks | PASS: 206/206 on each Node version with zero failures, skips, or cancellations. Independent post-fix review verifies single-result admission and exact active-action timer ownership; HTTP-R29/R30 are verified. |
| 2026-09-12 remediation | Acquisition-lock crash recovery and collection impact accounting | Seven Evidence controller/bundle/runner/live-adapter suites on Node 24.13.0 and checksum-verified portable Node 20.20.2; ten Node-24 and five Node-20 killed-child 12-way recovery repetitions; syntax, lint, ledger/release, generator-drift, and diff checks | PASS: author verification records 175 total, 173 pass, and 2 expected POSIX-only skips on each Node version, with all 15 dead-owner stress repetitions passing. Independent review repeated the 175-test matrix on each runtime with identical results, passed 45/45 Node-24 and 27/27 Node-20 focused lock-race assertions, passed 2/2 direct object-counter boundaries, and reconfirmed syntax, lint, ledger/release, generator-drift, and diff checks. EVID-R25/R26 are verified. |
| 2026-09-12 remediation | Final non-Evidence timer-boundary and release-documentation review | Ten HTTP, browser, campaign, mutation, recon, authorization-redaction, transport-boundary, and generated-connector suites on Node 24.13.0 and checksum-verified portable Node 20.20.2; five release/ledger suites on both runtimes; six production syntax checks on both runtimes; lint; all three generator drift checks; diff check | PASS: boundary matrix 278/278 and release/ledger matrix 36/36 on each Node version with zero failures, skips, or cancellations; syntax 6/6 on each runtime; lint reports 22 lenses/243 slugs; generators and diff check pass. Red-first review finds and fixes HTTP-R31 through HTTP-R34, WEB-R30/WEB-R31, and REL-02; independent post-fix review pending. |
| 2026-09-12 remediation | Non-Evidence timeout, observer, classifier, and byte-cleanup post-fix review | Eleven-suite boundary matrix: authenticated client, browser extension/bridge/injected runtime, campaign controller/ledger, mutation controller, recon client, authorization redaction, transport boundary, and generated connector on Node 24.13.0 and portable Node 20.20.2; five named release-wiring/provenance suites; ledger tests; eight production syntax checks on both runtimes; lint; all three generator drift checks; diff check | PASS: boundary matrix 304/304, release-wiring/provenance 30/30, and ledger 7/7 on each Node version with zero failures, skips, or cancellations; syntax 8/8 on each runtime; lint reports 22 lenses/243 slugs; generators and diff check pass. Red-first review fixes HTTP-R35 through HTTP-R39 and WEB-R32 through WEB-R39; independent post-fix review pending. |
| 2026-09-12 remediation | Post-freeze exposed-object and listener-boundary remediation | The same eleven-suite boundary matrix on Node 24.13.0 and portable Node 20.20.2; five named release-wiring/provenance suites; ledger tests; nine production syntax checks on both runtimes; lint; all three generator drift checks; diff check | PASS: boundary matrix 310/310, release-wiring/provenance 30/30, and ledger 7/7 on each Node version with zero failures, skips, or cancellations; syntax 9/9 on each runtime; lint reports 22 lenses/243 slugs; generators and diff check pass. Red-first regressions fix WEB-R40 and HTTP-R40; independent post-fix review pending. |
| 2026-09-12 remediation | Final browser post-invocation outcome remediation | The same eleven-suite boundary matrix on Node 24.13.0 and portable Node 20.20.2; five named release-wiring/provenance suites; ledger tests; nine production syntax checks on both runtimes; lint; all three generator drift checks; diff check | PASS: boundary matrix 313/313, release-wiring/provenance 30/30, and ledger 7/7 on each Node version with zero failures, skips, or cancellations; syntax 9/9 on each runtime; lint reports 22 lenses/243 slugs; generators and diff check pass. Red-first review fixes WEB-R41; independent post-fix review pending. |
| 2026-09-12 remediation | Independent final late HTTP/browser/release verification | Exact eleven-suite HTTP/browser boundary matrix, five release-wiring/provenance suites, ledger tests, and nine production syntax checks on Node 24.13.0 and portable Node 20.20.2; lint; all three generator drift checks; diff check | PASS: boundary matrix 313/313, release-wiring/provenance 30/30, ledger 7/7, and syntax 9/9 on each runtime with zero failures, skips, or cancellations; lint reports 22 lenses/243 slugs; topics, 84 benchmark cases, and capability documentation are in sync; diff check passes. Independent adversarial rereview verifies HTTP-R31 through HTTP-R40, WEB-R30 through WEB-R41, and REL-02. |
| 2026-09-12 final local gate | Full supported Node 24 suite | `npm.cmd test`; evidence `C:\Users\geral\Red Team\scratchpad\last-aperture-final-node24-0.14.1.log`, SHA-256 `e9c2a3671d1c0d86043ee987a9d837c905be9d8ee8733f5487807fb0d73033e6` | PASS: 3,244 tests; 3,240 passed; zero failures or cancellations; four expected Windows/POSIX skips. |
| 2026-09-12 final local gate | Full supported Node 20 suite | pinned portable Node 20.20.2 `--test`; evidence `C:\Users\geral\Red Team\scratchpad\last-aperture-final-node20-0.14.1.log`, SHA-256 `56b74ac393635a1f258db9259e4ad119d1dc62be45f6465a6476236cf3d6d726` | PASS: 3,232 tests; 3,203 passed; zero failures or cancellations; 29 expected runtime/platform skips. |
| 2026-09-12 final local gate | Static, conformance, dependency, and release checks | lint; all three generator checks; Python bounty conformance; offline npm audit; five release/provenance suites; 91 changed JavaScript syntax checks; `git diff --check` | PASS: 22 lenses/243 slugs; 84 benchmark cases; capability docs in sync; Python 38/38; audit v2 reports zero vulnerabilities across nine dependencies; release/provenance 30/30; syntax and diff clean. Audit evidence SHA-256 `f2df6f749196936a9ac24a9dee8c3bcc3a8301ad2368644c44b09c77bda3a81e`. |
| 2026-09-12 final local gate | Rebuilt package content and target-neutral byte scan | actual `npm pack`, clean extraction, inventory scan, configured residue scan, local-identity scan; generic-path focused tests on Node 24 and Node 20 | PASS: 817 files, 3,588,148 packed bytes, 14,052,489 unpacked bytes; zero excluded cache/history/remediation entries, target-specific residue terms, or local workstation identities; generic-path focused matrix 20/20 on each runtime. |
| 2026-09-12 release candidate | Rebased artifact and clean install | `C:\Users\geral\Red Team\release-artifacts\last-aperture-0.14.1\last-aperture-0.14.1.tgz`; SHA-256 sidecar; clean offline install under `scratchpad`; installed root CLI, capabilities, doctor, dependency and asset inventory | PASS: final artifact has 817 files, 3,588,981 packed bytes, 14,056,987 unpacked bytes, npm shasum `93abc4ab6c9097e6ce4c7fe57c0fded8915afa5b`, and SHA-256 `17319e04d6467e4eeafbe321dd62139a3f4287ecbb737a515002fb3fbea9f68d`; extracted byte scan finds no configured target residue or local identity; exact direct pins are `acorn@8.15.0`, `ajv@8.20.0`, `fast-check@4.9.0`, and `yaml@2.9.0`; installed package is v0.14.1, reports 32 capabilities, exits successfully through the installed CLI, and contains every required runtime asset. |
| 2026-09-12 hosted PR gate | GitHub Actions run `34715712032` for PR `#9`, head `d3157dc0fe770e7284f6451682b18b44213f316e` | FAIL: Node 24.20.0 completed 3,244 tests with 3,223 passed, 3 failed, and 18 skipped; Node 20.20.0 completed 3,232 tests with 3,188 passed, 1 failed, and 43 skipped. Both versions exposed the non-executable POSIX doctor fixture; Node 24.20.0 also exposed two non-transferable request-body regressions. The two live self-detached POSIX process tests and the cross-host Windows launcher-policy regression passed on both versions. |
| 2026-09-12 hosted-failure remediation | Dedicated request-body ownership, bounded preallocation, and cross-platform doctor fixture; four focused suites; independent HTTP rereview; complete sequential suites | PASS: focused matrix 87/87 on Node 24.20.0 and Node 20.20.2; independent HTTP matrix 148/148 on Node 24.20.0 and 32/32 on Node 20.20.2; full Node 24.20.0 suite 3,246 tests, 3,242 passed, zero failures, four expected skips; full Node 20.20.2 suite 3,234 tests, 3,205 passed, zero failures, 29 expected skips. Logs: `C:\Users\geral\Red Team\scratchpad\last-aperture-post-hosted-fix-node24-sequential-0.14.1.log` SHA-256 `8a4abb1b7bd2974f256653bfee3761c5e9707df20a8bd54fab67f188257ab7e9`; `C:\Users\geral\Red Team\scratchpad\last-aperture-post-hosted-fix-node20-sequential-0.14.1.log` SHA-256 `b596dfcdbf6d25173d0d906e2aa3f0177b470dc5b9f79793deb117d19c60932b`. Hosted Linux post-fix proof is recorded in the following row. |
| 2026-09-12 hosted post-fix gate | GitHub Actions run `34718095126` for PR `#9`, head `c963eb7c4b3c8d93635d12614767d722cb198e3f`; full suite on both runtimes; lint and all generator drift checks in the Node 24 job | PASS: Node 24.20.0 completed 3,246 tests with 3,228 passed, zero failures or cancellations, and 18 expected platform skips; Node 20.20.2 completed 3,234 tests with 3,191 passed, zero failures or cancellations, and 43 expected runtime, platform, or checkout skips. The doctor cross-platform fixture and both dedicated request-body detachment regressions pass on both hosted runtimes. Preserved logs: `C:\Users\geral\Red Team\scratchpad\last-aperture-hosted-run-34718095126-node24.log` SHA-256 `98064c180a4fe902cbf139c4dc9fdfbcbef9a4bf30a70c6c0b4f0738dc83df9a`; `C:\Users\geral\Red Team\scratchpad\last-aperture-hosted-run-34718095126-node20.log` SHA-256 `9615a22be7974a35e3d665653e5f77622ff4151e7ebb4993a544fba8916c4e6c`. |

## Completion checklist

- [x] Every finding above is `VERIFIED` or has an explicit, user-accepted external dependency.
- [x] Focused regression suites pass on the local supported Node versions.
- [x] `npm.cmd test` passes without failures or cancellations.
- [x] Lint, generated-file checks, Python conformance, and dependency audit pass.
- [x] GitHub pull-request checks run and pass.
- [x] Version is unique and consistent in source and package metadata.
- [x] Uploadable package is built and its SHA-256 is recorded.
- [x] Codex and Claude installations resolve to the final committed tree.
- [ ] Commits are pushed and the reviewed pull request is merged.
